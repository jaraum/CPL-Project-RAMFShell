# README 样例测试

这些文件对应项目 README 中的样例 1 至 5，互不影响，也不会覆盖仓库根目录的 `main.c`。

在仓库根目录运行：

```sh
./sample/run.sh        # 依次运行全部样例
./sample/run.sh 1      # 只运行样例 1
```

样例包含 `assert`；某项断言失败时，程序会立即退出，说明对应功能尚未实现或实现不符合要求。
