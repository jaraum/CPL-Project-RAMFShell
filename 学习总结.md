# RAM File System Shell：实现与学习总结

## 这次项目实现了什么

项目在内存中维护一棵文件树，并提供接近 POSIX 的文件操作：创建、打开、读写、定位、删除文件和目录；在它之上实现了简化 Shell 的 `ls`、`cat`、`mkdir`、`touch`、`echo`、`which` 与 PATH 环境变量读取。

## 保持原结构的设计

- `fs/ramfs.c`：文件系统唯一的状态拥有者。`root` 是树根，`fdesc` 是固定大小的文件描述符表。
- `sh/shell.c`：仅使用 `ramfs.h` 的公开接口和 `find`，不直接维护文件系统状态。
- `include/*.h`：保持既有公开 API，不把内部的路径分割、节点扩容等辅助函数暴露出去。

文件系统内部把辅助逻辑分成四类：路径合法性校验、节点查找/增删、文件描述符权限校验、递归释放与失效描述符处理。这样每个 API 函数只保留自身的业务规则，阅读起来更清楚。

## 关键实现取舍

1. **目录使用动态子节点数组**：访问一个目录项是线性查找，代码直观且适合本项目；若追求更大规模性能，可替换为哈希表。
2. **偏移量属于文件描述符**：同一文件被多次打开时，每个 fd 都有独立读写位置，这是文件系统抽象里很重要的边界。
3. **写入允许空洞**：`seek` 到文件末尾以后再写时，会扩容并用 `0` 填充中间区域。
4. **删除时使关联 fd 失效**：这比释放节点后留下悬空指针更安全，也能让后续读写稳定地失败。
5. **Shell 的 PATH 用字符串保存**：解析 `.bashrc` 中的 `export PATH=...`，支持把 `$PATH` 展开到新值中；这是在功能简单与代码量之间的合适平衡。

## 做项目时值得养成的习惯

- 先明确所有权：谁创建内存、谁释放内存、删除后还有谁可能引用它。
- 先写不变量：例如“根一定是目录”“有效 fd 一定指向活节点”“目录项名称必须合法”。实现时持续维护它们。
- 边界条件和主流程一样重要：空路径、根目录、重复创建、穿过普通文件访问子路径、无效 fd、权限不匹配都应先判断。
- 避免把“查找”“修改”“输出报错”揉成一个大函数。文件系统负责状态，Shell 负责呈现，是很自然的分层。
- 每新增一个资源（数组、字符串、节点）时，同时想好失败分支如何回收；C 项目里这通常决定了代码是否可靠。

## 后续可以继续练习

- 为节点加入父指针，简化删除和路径回溯。
- 用哈希表加速大目录下的文件查找。
- 增加 `cd`、交互式命令解析、重定向和管道。
- 为每个 API 写单元测试，并使用 AddressSanitizer/UndefinedBehaviorSanitizer 检查内存问题。
